# Keyframes

Mixin for creating `@keyframes`

## Installation

```
fonzie install keyframes
```

## Usage

```scss
@include keyframes(fade-in) {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
```