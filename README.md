# Keyframes

Mixin for creating `@keyframes`

## Installation

```
bower install fonzie-keyframes
```

## Usage

```scss
@include fz-keyframes(fade-in) {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
```